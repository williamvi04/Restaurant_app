import React from "react";
import ReactDOM from "react-dom";
import "../src/all-styles.css";
import { App } from "./App/App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
